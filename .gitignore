# «index.html» тут НЕ ігнорується: саме його роздає GitHub Pages.
# Він збирається з «src/» командою: python "build.py"
# «tests.html» — теж збірка, але вона нікуди не публікується.
tests.html

# Службове
__pycache__/
*.pyc
*.json.tmp

# Експорти оцінок кладуться сюди вручну; це резервні копії даних, а не код.
резервні/
